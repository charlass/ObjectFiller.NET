
- Klasse "ObjectFiller" hat gleichen Namen wie der Namespace 
	=> einen der Namen ändern

- Als User will nicht zig Namespaces einbinden, vor allem nicht ohne Re# ;-) 
	=> alle Namespaces im ObjectFiller flachklopfen?

- Schwer zu lesen: RandomizerForProperty(randomizer, property)
	=> Parameter vertauschen??
